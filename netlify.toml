[build]
  command = "npm run build:prod && cp dist/index.html dist/404.html"
  publish = "dist"

[build.environment]
  VITE_BASE_URL = "/"

# All pages are pre-rendered by SSG — no catch-all rewrite needed.
# Netlify auto-serves 404.html for unknown URLs.
